# =========================================================
# KIVU ADVISORY - BACKEND CONTAINER ENV EXAMPLE
# =========================================================
# Copy this file to deployment/env/backend.env on the server.
# Never commit the real backend.env file.
#
# This file is used by the backend Docker container.
# Production database is Neon PostgreSQL.
# Production document storage is Cloudflare R2.
# =========================================================

# =========================================================
# Application
# =========================================================
APP_NAME=Kivu Advisory Backend
APP_ENV=production
APP_TIMEZONE=Africa/Kigali

# =========================================================
# Server
# =========================================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
API_VERSION=v1
API_BASE_PATH=/api/v1

# =========================================================
# Public URLs
# =========================================================
FRONTEND_PUBLIC_URL=https://yourdomain.com
BACKEND_PUBLIC_URL=https://yourdomain.com

# =========================================================
# CORS
# =========================================================
CORS_ALLOWED_ORIGINS=https://yourdomain.com
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Authorization,Content-Type,Accept,Origin
CORS_ALLOW_CREDENTIALS=true

# =========================================================
# Neon PostgreSQL
# =========================================================
# Get this value from Neon dashboard.
# Production must use sslmode=require.
DATABASE_URL=

DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=10
DB_CONN_MAX_LIFETIME_MINUTES=30
DB_CONN_MAX_IDLE_TIME_MINUTES=10

# Path inside backend container
MIGRATIONS_PATH=file://db/migrations

# =========================================================
# Authentication / JWT
# =========================================================
JWT_SECRET=replace-with-long-random-production-secret
JWT_ISSUER=kivu-advisory
JWT_AUDIENCE=kivu-advisory-users
JWT_ACCESS_TOKEN_TTL_MINUTES=60
JWT_REFRESH_TOKEN_TTL_DAYS=7

# =========================================================
# Password security
# =========================================================
PASSWORD_MIN_LENGTH=8
BCRYPT_COST=12

# =========================================================
# Storage driver
# =========================================================
# Allowed values:
# local
# r2
#
# Production should use r2.
STORAGE_DRIVER=r2

# Local storage fallback only for development/testing
LOCAL_UPLOAD_DIR=tmp/uploads-for-local-development

# =========================================================
# Cloudflare R2 private document storage
# =========================================================
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=kivu-advisory-prod-documents
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_REGION=auto

# Keep empty for private confidential documents.
R2_PUBLIC_BASE_URL=

# =========================================================
# Upload security rules
# =========================================================
MAX_UPLOAD_SIZE_MB=20
ALLOWED_UPLOAD_EXTENSIONS=pdf,doc,docx,xls,xlsx,png,jpg,jpeg
ALLOWED_UPLOAD_MIME_TYPES=application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,image/png,image/jpeg

# =========================================================
# Email / SMTP
# =========================================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM_NAME=Kivu Advisory
SMTP_FROM_EMAIL=no-reply@yourdomain.com
SMTP_USE_TLS=true

# =========================================================
# Notifications
# =========================================================
SEND_EMAIL_NOTIFICATIONS=true
NOTIFY_ADMIN_ON_NEW_REQUEST=true
NOTIFY_ADMIN_ON_DOCUMENT_UPLOAD=true
NOTIFY_CLIENT_ON_STATUS_CHANGE=true

# =========================================================
# Admin bootstrap
# =========================================================
# Used only for creating the first admin account.
# Change this password immediately after first login.
ADMIN_FULL_NAME=System Administrator
ADMIN_EMAIL=yveskwizera154@gmail.com
ADMIN_PASSWORD=Admin@MACC2024
ADMIN_PHONE=0784545454

# =========================================================
# Logging
# =========================================================
LOG_LEVEL=info
LOG_FORMAT=json
